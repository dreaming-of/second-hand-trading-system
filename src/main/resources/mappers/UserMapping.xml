<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chl.campussecondhandtradingsystem.Dao.IUserDao">
    <sql id="selectField">
        user_id, username, password, student_number, phone_number, email, headerImg
    </sql>

    <sql id="insertField">
        username, password, student_number, phone_number, email, headerImg
    </sql>

    <select id="findUser" parameterType="user" resultType="user">
        select
        <include refid="selectField"></include>
        from users where username = #{username} and password = #{password};
    </select>

    <insert id="insertUser" parameterType="user" keyProperty="user_id">
        insert into users(<include refid="insertField"></include>)
        values(#{username}, #{password}, #{student_number}, #{phone_number}, #{email}, #{headerImg});
    </insert>

    <select id="findUserByStudentNumber" resultType="int">
        select count(user_id) from users where student_number = #{student_number};
    </select>

    <select id="findUserById" resultType="user">
        select
        <include refid="selectField"></include>
        from users
        where user_id = #{id}
    </select>

    <!--    select by student_number    -->
    <select id="">
        select
        <include refid="selectField"></include>
        from users
        where student_number = #{student_number}
    </select>

    <update id="updateHeader">
        update users set headerImg = #{param2} where user_id = #{param1}
    </update>
</mapper>