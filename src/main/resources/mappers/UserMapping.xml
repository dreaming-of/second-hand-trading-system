<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chl.campussecondhandtradingsystem.Dao.IUserDao">
    <select id="findUser" parameterType="user" resultType="user">
        select user_id, username, student_number, phone_number, email, img from users where username = #{username} and password = #{password};
    </select>

    <insert id="insertUser" parameterType="user">
        insert into users(username, password, student_number, phone_number, email) values(#{username}, #{password}, #{student_number}, #{phone_number}, #{email});
    </insert>

    <select id="findUserByStudentNumber" parameterType="String" resultType="int">
        select count(user_id) from users where student_number = #{id};
    </select>
</mapper>